export class AwsS3 {
    // onModuleInit(){ connectin к s3 }
    getFile() {
        // по api получаем файл
    }

    readFile() {
        // this.getFile получаем файл не сохроняем, она в оперативной памяти и по ней считаем
    }
}